// export const initOnClickRemoveAddFavorites = () => {
//   const favorites = document.querySelectorAll(".fa-heart");
//   favorites.forEach((favorite) => {
//     favorite.addEventListener('click', (event) => {
//       if (favorite.classList.contains("fas")) {
//         const x_not_favorite = document.getElementById("not-favorite");
//         const parentNode = x_not_favorite.parentNode
//         parentNode.removeChild(x_not_favorite)
//         parentNode.insertAdjacentHTML("beforeend", `<%= link_to favorites_path(dest: destination[:id]), method: :post, id: "favorite" do %>
//                                                       <i class="fas fa-heart favorite"></i>
//                                                     <% end %>`);

//       }
      else {
        // const x_favorite = document.getElementById("favorite");
        // const parentNode = x_favorite.parentNode
        // parentNode.removeChild(x_favorite)
        // parentNode.insertAdjacentHTML("beforeend", `<%= link_to list_favorites_path(Favorite.where(user: current_user).where(destination: destination[:id])[0][:id]), method: :delete, id: "not-favorite" do %>
        //                                               <i class="fas fa-heart favorite"></i>
        //                                             <% end %>`);
      }
//     });
//   });
// };

