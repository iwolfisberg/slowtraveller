<div class="main-container">
  <div>
    <h1>Slow traveller</h1>
  </div>

  <div class="form">
    <div class="col-sm-12">
      <div id="destinations">
        <h3>You are a slow traveller</h3>
        <%= render 'shared/form_research' %>
      </div>
    </div>
  </div>

  <div class="text">
    <% if params["/destinations"] == nil %>
      <%= render 'shared/text_index' %>
    <% else %>
      <div class="main-cards">
        <% @destinations.each do |destination| %>
          <%= link_to destination_path(destination[1][0]), id:"link_destination" do %>
            <div class="destination-card">
              <div class="destination-photo">
                <%= image_tag destination[1][0].photo_url, height: 120, crop: :scale, alt: destination[1][0].name, class: "destination-image" %>
              </div>
              <div class="destination-infos">
                <h2><%= destination[1][0].name %> (<%= destination[1][0].country %>)</h2>
                <p><%= destination[1][1][:duration] %></p>
                <p><%= destination[1][1][:carbon] %> kg CO2</p>
                <p><%= destination[1][1][:connections] %> connections</p>
              </div>
            </div>
          <% end %>
        <% end %>
      </div>
    <% end %>
  </div>
</div>
